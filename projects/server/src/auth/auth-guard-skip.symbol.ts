import { AuthGuardSkip } from './auth-guard-skip.decorator';

/**
 * A symbol to store metadata for {@link AuthGuardSkip}
 */
export const AUTH_GUARD_SKIP = Symbol('auth-guard-skip');

AuthGuardSkip;
