import { FlipDirective } from './flip.directive';

describe('FlipDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipDirective();
    expect(directive).toBeTruthy();
  });
});
