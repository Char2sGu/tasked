import { ConfirmationDirective } from './confirmation.directive';

describe('ConfirmationDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmationDirective();
    expect(directive).toBeTruthy();
  });
});
