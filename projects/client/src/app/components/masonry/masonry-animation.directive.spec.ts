import { MasonryAnimationDirective } from './masonry-animation.directive';

describe('MasonryAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new MasonryAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
