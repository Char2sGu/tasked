import { RouterEventsDirective } from './router-events.directive';

describe('RouterEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterEventsDirective();
    expect(directive).toBeTruthy();
  });
});
