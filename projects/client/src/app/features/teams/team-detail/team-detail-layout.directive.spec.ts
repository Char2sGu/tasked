import { TeamDetailLayoutDirective } from './team-detail-layout.directive';

describe('TeamDetailLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamDetailLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
