import { EqualsValidator } from './equals.validator';

describe('EqualsValidator', () => {
  it('should create an instance', () => {
    const directive = new EqualsValidator();
    expect(directive).toBeTruthy();
  });
});
