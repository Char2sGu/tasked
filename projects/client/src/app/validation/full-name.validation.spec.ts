import { FullNameValidation } from './full-name.validation';

describe('FullNameValidation', () => {
  it('should create an instance', () => {
    const directive = new FullNameValidation();
    expect(directive).toBeTruthy();
  });
});
