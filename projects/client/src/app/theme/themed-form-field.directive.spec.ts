import { ThemedFormFieldDirective } from './themed-form-field.directive';

describe('AutoFormFieldColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemedFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
