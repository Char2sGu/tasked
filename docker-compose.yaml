version: "3.9"

services:
  backend:
    build: tasked-backend
    env_file:
      - tasked-backend/.env
    environment:
      - PORT=3000
    ports:
      - 3000:3000
    volumes:
      - data:/app/data/
  frontend:
    build: tasked-frontend
    ports:
      - 80:80

volumes:
  data:
